const supertest = require("supertest");
const app = require("../server");

describe("User route", () => {
  it("Post ", async () => {
    const response = await supertest(app);
    expect()
  });
});
